<div class="container">
    {{ form_start(form) }}
        <div class="row mb-3">
            <div class="form-floating">
                {{ form_row(form.ForfaitEtape) }}
            </div>
        </div>
        <div class="row mb-3">
            <div class="form-floating">
                {{ form_row(form.ForfaitKilometrique) }}
            </div>
        </div>
        <div class="row mb-3">
            <div class="form-floating">
                {{ form_row(form.ForfaitNuitee) }}
            </div>
        </div>
        <div class="row mb-3">
            <div class="form-floating">
                {{ form_row(form.ForfaitRestaurant) }}
            </div>
        </div>
        <button class="btn btn-primary">{{ button_label|default('Valider') }}</button>
    {{ form_end(form) }}
</div>
