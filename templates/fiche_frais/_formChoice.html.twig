<div class="container">
    {{ form_start(form) }}
    <div class="row mt-5 ml-4">
        <div class="form-floating">
            <h6>{{ form_row(form.mois) }}</h6>
        </div>
        <button class="btn btn-primary ml-3">{{ button_label|default('Valider') }}</button>
    </div>
    {{ form_end(form) }}
</div>